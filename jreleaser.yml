project:
  name: klokwrk-project
  license: Apache-2.0

signing:
  active: NEVER

release:
  github:
    username: dmurat
    overwrite: true
    draft: true
    skipTag: true
    changelog:
      enabled: false

files:
  globs:
    - directory: artifacts-klokwrk-tool-gradle-source-repack
      include: "*.*"
