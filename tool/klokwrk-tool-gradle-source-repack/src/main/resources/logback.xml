<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <withJansi>false</withJansi>
    <!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>%cyan(%d{HH:mm:ss.SSS}) %gray([%thread]) %highlight(%-5level) %magenta(%logger{36}) - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="${rootLogLevel:-WARN}">
    <appender-ref ref="STDOUT"/>
  </root>

  <logger name="org.klokwrk.tool.gradle.source.repack" level="${toolLogLevel:-${rootLogLevel:-WARN}}" />
  <logger name="io.micronaut.http.client" level="${micronautHttpClientLogLevel:-${rootLogLevel:-WARN}}"/>
  <logger name="io.netty.util.internal.PlatformDependent0" level="INFO"/>
</configuration>
